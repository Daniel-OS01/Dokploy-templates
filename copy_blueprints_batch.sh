#!/bin/bash
for dir in blueprints/a*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/b*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/c*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/d*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/e*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/f*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/g*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/h*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/i*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/j*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/k*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/l*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/m*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/n*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/o*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/p*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/q*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/r*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/s*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/t*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/u*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/v*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/w*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/x*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/y*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
for dir in blueprints/z*; do
  if [ -d "$dir" ]; then
    cp -r "$dir" examples/
  fi
done
